# Zentrix Mobile App

Expo Router mobile app for the Zentrix customer experience.

## Run

```bash
npm install
cp .env.example .env
npm run start
```

Set `EXPO_PUBLIC_API_URL` to the backend API base URL. On Android emulator, use `http://10.0.2.2:5000` instead of `localhost`.
